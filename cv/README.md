Drop CV PDFs here (e.g. `cv-en.pdf`, `cv-pt.pdf`). `build.ps1` copies every
`*.pdf` in this folder into `docs/` outright when the site is built — a
redirect stub can't stand in for a PDF. See CONTEXT.md "Legacy URL".
