[pytest]
testpaths = tests
markers =
    offline: teste que não faz nenhuma chamada de rede (roda por padrão)
    online: teste de integração que acessa a rede real do BCB (não roda por padrão; use -m online)
addopts = -m "not online"
